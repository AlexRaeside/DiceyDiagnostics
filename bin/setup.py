from setuptools import setup, find_packages

setup(
    name="DiceyDiagnostics",      # Name of your package
    version="0.1.0",        # Version of your package
    packages=find_packages() # List of packages to include (or use `find_packages()` if there are submodules)
)